# Read by `release` (~/.claude/bin/release).

# `navi` on crates.io is an unrelated project (the interactive cheatsheet tool,
# at 2.x). This navi ships through the Homebrew tap only, so the release script
# and `release --audit` should not treat "not on crates.io" as a missed step.
NO_PUBLISH=true
